<project name="deploy-ejb-asynchotel" default="deploy" basedir="./build-file">
    <property environment="env"/>

    <property name="deploy.dir" value="D:\wildfly-10.1.0.Final\standalone\deployments\"/>

    <property name="asynchotel-warname" value="asynchotel.war"/>
    <property name="asynchotel-jarname" value="asynchotel-ejb.jar"/>
    <property name="asynchotel-earname" value="asynchotel.ear"/>
    <property name="srcejb-dir" value="./asynchotel-ejb/src/"/>
    <property name="srcwar-dir" value="./asynchotel-war/src/java"/>
    
    <property name="build-asynchotel-war_war" value="${basedir}/asynchotel_war" />
    <property name="build-asynchotel-ejb_jar" value="${basedir}/asynchotel_jar" />
    <property name="build-asynchotel-lib" value="${basedir}/lib" />
    
    <property name="buildEar.dir" value="${basedir}/ear"/>
    <property name="webcontent.dir" value="../asynchotel-war/web"/>
    
    <property name="srcproperties.dir" value="../asynchotel-ejb/src/java/resources"/>


    <path id="classpath">
        <fileset dir="${basedir}\lib">
            <include name="**/*.jar" />
        </fileset>
    </path>
	
	

	
    <target name="clean" >
        <echo> 
            ---- Compilation asynchotel ----------------
        </echo>
        <echo>JDK ${ant.java.version}  ${ant.version}  </echo>
        <echo> clean </echo>
        
        <delete file="${build-asynchotel-ejb_jar}" />
        <delete file="${build-asynchotel-war_war}" />
        <delete file="${build-asynchotel-lib}" />
        <delete file="${basedir}/${asynchotel-earname}" />
        <delete file="${buildEar.dir}/${asynchotel-earname}" />
		
        <echo> clean done </echo>
    </target>
	
    <tstamp>
        <format property="dateDuJour" pattern="dd/MM/yyyy hh:mm aa" />
    </tstamp>
	
    <target name="init" depends="clean">
        <echo> initialisation ${dateDuJour} </echo>
        
        <mkdir dir="${build-asynchotel-war_war}"/>
        <mkdir dir="${build-asynchotel-war_war}/WEB-INF"/>
        <mkdir dir="${build-asynchotel-war_war}/WEB-INF/lib"/>
        <mkdir dir="${build-asynchotel-war_war}/WEB-INF/classes"/>
        <mkdir dir="${build-asynchotel-war_war}/META-INF"/>
        <mkdir dir="${build-asynchotel-war_war}/WEB-INF/classes/META-INF"/>
        
        <mkdir dir="${build-asynchotel-ejb_jar}"/>
		
		
        <echo file="${webcontent.dir}/dateBuild.txt" append="false" message="${dateDuJour}" />
        
        <copy todir="${build-asynchotel-war_war}/">
            <fileset dir="${webcontent.dir}" excludes="META-INF/**"/>
        </copy>
        <copy todir="${build-asynchotel-war_war}/META-INF">
            <fileset dir="${webcontent.dir}/WEB-INF/META-INF" />
        </copy>
        <copy todir="${build-asynchotel-war_war}/WEB-INF/lib/">
            <fileset dir="${basedir}\lib" />
        </copy>

        <copy file="../asynchotel-ejb/src/java/project.properties" tofile="${build-asynchotel-ejb_jar}/project.properties" />
		<copy file="../asynchotel-ejb/src/java/apj.properties" tofile="${build-asynchotel-ejb_jar}/apj.properties" />
		<copy file="../asynchotel-ejb/src/java/resources/text.properties" tofile="${build-asynchotel-ejb_jar}/java/resources/text.properties" />
		<copy file="../asynchotel-ejb/src/java/resources/text_zh.properties" tofile="${build-asynchotel-ejb_jar}/java/resources/text_zh.properties" />
        <echo>  initialisation done </echo>
        <copy todir="${build-asynchotel-war_war}/WEB-INF">
            <fileset dir="./../asynchotel-war/web/WEB-INF" includes="**/*.xml" />
        </copy>
    </target>
	
    <target name="compile" depends="init">
        <echo> compilation </echo>	
        <javac srcdir="../asynchotel-ejb/src/java" destdir="${build-asynchotel-ejb_jar}" nowarn="true" debug="on" deprecation="off" classpathref="classpath" includeantruntime="false" encoding="iso-8859-1">
        </javac>
        <echo> compilation done </echo>	
    </target>
    
    <target name="buildEjbJar" depends="compile">
        <echo>Build EJB JAR</echo>
        <jar basedir="${build-asynchotel-ejb_jar}" destfile="${buildEar.dir}/${asynchotel-jarname}" />
        <copy file="${buildEar.dir}/${asynchotel-jarname}" todir="${build-asynchotel-war_war}/WEB-INF/lib/">
        </copy>
        <echo>End Build EJB JAR</echo>
    </target>

    <target name="compileWar" depends="buildEjbJar">
        <echo> compilation </echo>
        <javac srcdir="../asynchotel-war/src/java" destdir="${build-asynchotel-war_war}/WEB-INF/classes/" nowarn="true" debug="on" deprecation="off" classpathref="classpath" includeantruntime="false" encoding="iso-8859-1">
            <classpath>
                <pathelement location="${buildEar.dir}/${asynchotel-jarname}" />
            </classpath>
        </javac>
        <echo> compilation done</echo>
    </target>


    <target name="copieProperties" depends="compileWar">
        <echo> Copie des properties</echo>
        <copy file="${srcproperties.dir}/text.properties" todir="${build-asynchotel-war_war}/WEB-INF/classes">
        </copy>
        <copy file="${srcproperties.dir}/text_zh.properties" todir="${build-asynchotel-war_war}/WEB-INF/classes">
        </copy>
        <echo> Fin de la copie </echo>
    </target>
	
	
    <target name="deploy" depends="copieProperties">	
        <echo>${env.JBOSS_HOME}</echo>
        <!--<echo> deployement ear </echo>-->
        <!--                <copy todir="${deploy.dir}">
            <fileset dir="${build.dir}" >
                <include name="${ketrika-earname}"/>
            </fileset>	
        </copy>-->
        <copy todir="${deploy.dir}/asynchotel.war">
            <fileset dir="${build-asynchotel-war_war}" >
                <include name="**/*"/>
            </fileset>	
        </copy>
        <touch file="${deploy.dir}/asynchotel.war.dodeploy"/>
        <echo> deployement exploded war done </echo>	
        <echo> 
            Compilation asynchotel GENIUS_ART_C [DONE]----------------
        </echo>
		
    </target>	
	
	

</project>
